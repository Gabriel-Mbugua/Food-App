import React from 'react';
import {
  StyleSheet,
  Text,
  View,
  Image,
  SafeAreaView,
  Button,
  ScrollView,
  StatusBar,
  Platform,
  FlatList,
} from 'react-native';

const ProfileScreen = ({navigation, route}) => {
  return (
    <SafeAreaView>
      <View>
        <Text>Profile!</Text>
      </View>
    </SafeAreaView>
  );
};

export default ProfileScreen;
